{
  "extends": "./node_modules/@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    // React Native 0.86 uses the automatic JSX runtime — no need for
    // `import React from 'react'` in every file. Override the base
    // config's "react-native" jsx mode which expects explicit imports.
    "jsx": "react-jsx",
    "paths": {
      "react-native-mapsforge-vtm-ext-path-color-ramp": ["../src/index"]
    }
  },
  "include": ["src", "../src"]
}
